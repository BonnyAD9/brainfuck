{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/brainfuck",
            "args": [ "../speed-test.b" ],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Makefile",
            "stdio": "../input",
            "env": {
                "ASAN_OPTIONS": "detect_leaks=0"
            }
        }
    ]
}
